{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('timeFramePage.title') }}</h1>

    <div>
        <p>{{ __('timeFramePage.intro') }}</p>

        <form method="post">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            {{ textInput('timeFramePage.startDate', 'timeFramePage.startDate', { hint: 'timeFramePage.startDateExample' }) }}
            {{ textInput('timeFramePage.endDate', 'timeFramePage.endDate', { hint: 'timeFramePage.endDateExample' }) }}
            {{ formButtons('timeframePage.nextButton') }}
        </form>
    </div>

{% endblock %}